# This line is a comment
# To create a hello world program

println("hello world")

# or 

print("hello world\n")

# the print will not start a newline (unlike println)so must be added in the string with \n

# the var function takes an identifier and anything and ties the identifier to it on the heap
var(x:18)

# you must append a colon to an identifier for it to use what it is tied to
println(x:)


if (
    (x: = 9)
    {
        var(x:20)
    }
    {
        var(x:10)
    }
)

println(x:)

var(append: {concat() println()})

("hello world, " "this is a hold up!".append)

("hello world, " "so is this!".append)

